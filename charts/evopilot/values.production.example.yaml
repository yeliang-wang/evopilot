image:
  repository: ghcr.io/yeliang-wang/evopilot
  tag: "6.2.0"
  pullPolicy: IfNotPresent

dashboard:
  enabled: true
  image:
    repository: ghcr.io/yeliang-wang/evopilot-dashboard
    tag: "3.1.0"
    pullPolicy: IfNotPresent

auth:
  existingSecret: evopilot-prod-secrets
  tokens: ""
  users: ""

postgres:
  enabled: true
  database: evopilot
  user: evopilot
  storage:
    size: 50Gi

persistence:
  enabled: true
  size: 100Gi

runtime:
  runMode: prod
  logLevel: info
  loopWorkerId: evopilot-production-worker
  hostWorkspaceMount: ""

resources:
  requests:
    cpu: 500m
    memory: 1Gi
  limits:
    cpu: "2"
    memory: 4Gi

ingress:
  enabled: true
  className: nginx
  annotations: {}
  hosts:
    - host: evopilot.example.com
      paths:
        - path: /
          pathType: Prefix
  tls: []
