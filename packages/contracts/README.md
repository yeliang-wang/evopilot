# @evopilot/contracts

Shared EvoPilot API, CLI, runtime, and package boundary contracts.

This package is published for the EvoPilot CLI and integration clients. It does not execute EvoPilot workflows by itself; server-side EvoPilot APIs remain the system of record for RBAC, tenant/workspace scope, approval gates, source closure, release policy, and audit.
