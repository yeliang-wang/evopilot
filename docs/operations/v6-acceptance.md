# Runtime 6 And Expert 2 Acceptance

Local source validation is not release acceptance. Counted acceptance must install exact private Runtime 6.0.0 and Evolution Expert 2.0.0 Candidate artifacts in fresh isolation and bind every result to both artifact digests.

The generated `governance/acceptance/v6-completion-contract.json` contains 253 criterion-specific validators across current, inherited, and E2E obligations. `runtime-6.0.0-expert-2.0.0-cross-acceptance-map.json` binds ten cross-product journeys.

Required terminal evidence includes Lifecycle CRUD and concurrency, restart/tenancy/security negatives, real published HarnessBundle consumption, exact external Agent Runtime execution, five generated Host bundles, Codex and Claude Code conformance, designated-human WorkBuddy declarations, generic MCP, headless recovery, declaration-only DataRig/EvoPilot/evopilot-harness/unknown-project cases, both legacy Suites absent, impact closure, `NO_REGRESSION`, and a 5400-second active soak.

Completion requires 100 percent passed with evidence. Failed, pending, stale, warning, generic, and unmapped counts must all be zero; exact installed Candidate pair verification and every cross row must pass; legacy Suite invocation count must be zero. Release remains a later independent authorization.
