# EvoPilot Runtime 6.2.0 — First-Run LLM Readiness

Status: accepted release scope. Public release evidence is recorded in the governed Runtime 6.2.0 Target after publication and independent public readback.

Runtime 6.2.0 makes an explicitly selected, live-preflight governed workspace LLM Profile a hard prerequisite for normal operation. It introduces digest-bound `RuntimeReadiness` and `WorkspaceLlmDefaultBinding` records, a setup-only production state, fail-closed degradation, explicit v6.1 migration, MCP/API/CLI setup surfaces, and architecture documentation that separates Host LLM, Runtime LLM, and external Agent Model.

The accepted release proves fresh install, headless setup, upgrade ambiguity, Profile and SecretRef drift, preflight expiry, repair, tenant/workspace isolation, no hidden fallback, README architecture, and compatibility with Evolution Expert 2.2.0 on the exact frozen release build. Learning Interoperability remains deferred intact to Runtime 6.3.0.
